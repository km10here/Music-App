import 'package:flutter/material.dart';

ThemeData getcolorTheme() {
  return ThemeData(
      scaffoldBackgroundColor: Colors.amber,
      appBarTheme: AppBarTheme(
          backgroundColor: Colors.greenAccent, foregroundColor: Colors.blue));
}
